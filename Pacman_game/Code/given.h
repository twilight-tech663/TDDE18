/*
 * This is a small wrapper so that the student code shouldn't need to be
 * changed to be added to the game.  Without this you would have to modifify
 * which files are included in the students' code.
 */
#include "constants.h"
#include "pacman.h"
